{
  "name": "darkfactory-paper",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun scripts/watch.ts",
    "publication": "bun scripts/publication.ts",
    "test": "bun run typecheck && bun test tests",
    "typecheck": "bun x tsc -p tsconfig.json",
    "lint": "biome lint scripts tests",
    "format": "biome format --write scripts tests package.json tsconfig.json biome.json types",
    "format:check": "biome format scripts tests package.json tsconfig.json biome.json types",
    "check": "bun run test && bun run lint && bun run format:check && bun run publication -- --check"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.13",
    "@types/bun": "1.3.14",
    "turndown": "7.2.4",
    "turndown-plugin-gfm": "1.0.2",
    "typescript": "5.9.2"
  }
}
