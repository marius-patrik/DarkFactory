import type { FC } from "react";

export const WebShell: FC = () => {
  return (
    <div>
      <h1>DarkFactory Web</h1>
    </div>
  );
};

export const DarkFactoryShell = WebShell;
export const App = WebShell;
