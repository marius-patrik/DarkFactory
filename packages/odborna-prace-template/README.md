# odborna-prace-template

Internal Typst package used by this repository.

This package is the source of truth for thesis layout, review markers, publication
profiles, terminology rendering, and word-count support. It replaces the former
standalone `template-OdbornaPrace` repository; the package evolves together with
the thesis while remaining separated from manuscript content.

Entrypoint: `src/lib.typ`.
