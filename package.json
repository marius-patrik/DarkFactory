{
  "name": "darkfactory-workspace",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "capabilities/*",
    "harness"
  ],
  "scripts": {
    "test": "bun run --cwd harness test",
    "typecheck": "bun run --cwd harness typecheck",
    "check": "bun run --cwd harness check",
    "docs": "bun scripts/build-docs.ts",
    "format:check": "BASE=\"$(bun scripts/get-base-branch.mjs)\"; if ! git rev-parse --verify \"$BASE\" > /dev/null; then echo \"Base branch $BASE not found\"; exit 1; fi; cd harness && git diff --name-only \"$BASE\" -- . | grep -E '\\.ts$|\\.tsx$|\\.js$|\\.mjs$' | xargs -r bun x biome ci"
  }
}
