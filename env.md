# Environment Variables

The following environment variables are used for repository and pipeline configuration.

| Variable | Description |
| :--- | :--- |
| `DF_BASE_SHA` | The commit SHA of the base branch to compare against (e.g., for drift detection). |
| `DF_CONFIG_PATH` | The path to the repository configuration file (default: `repo.dfconfig`). |
